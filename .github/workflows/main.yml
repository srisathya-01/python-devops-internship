name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install rsync
        run: sudo apt-get update && sudo apt-get install -y rsync

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to production via rsync
        env:
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          PROD_DEPLOY_HOST: ${{ secrets.PROD_DEPLOY_HOST }}
          APP_DIR: ${{ secrets.APP_DIR }}
        run: |
          rsync -avz --delete ./ \
          ${DEPLOY_USER}@${PROD_DEPLOY_HOST}:${APP_DIR}/

      - name: Restart production service
        env:
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          PROD_DEPLOY_HOST: ${{ secrets.PROD_DEPLOY_HOST }}
        run: |
          ssh -o StrictHostKeyChecking=no \
          ${DEPLOY_USER}@${PROD_DEPLOY_HOST} "sudo systemctl restart myapp"

      - name: Production health check
        env:
          PROD_DEPLOY_HOST: ${{ secrets.PROD_DEPLOY_HOST }}
        run: |
          curl -fsS --max-time 10 http://${PROD_DEPLOY_HOST}/health
