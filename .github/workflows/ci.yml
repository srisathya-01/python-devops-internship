name: CI

on:
  pull_request:
    branches:
      - main
      - staging
      - feature/**

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt || echo "No requirements file"

      - name: Lint
        run: |
          echo "Running linter"
          flake8 . || echo "Lint failed"

      - name: Unit tests
        run: |
          echo "Running tests"
          pytest || echo "Tests failed"
