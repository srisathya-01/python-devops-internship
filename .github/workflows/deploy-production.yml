name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production

    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        run: |
          echo "Deploying to PRODUCTION"
          echo "Production deploy successful"
 http://$PROD_DEPLOY_HOST/health || exit 1
